// Las reglas de la agenda viven en /reglas.js, en la raíz, para que el mismo
// archivo lo usen las funciones del servidor y el navegador. Así no hay dos
// versiones de la misma regla.
export * from '../reglas.js';
